#!/usr/bin/venom

class Animal <- object

  attr name::string
  attr type::string = "animal"
  attr weight::int

  def self(name::string) =
    self.name = name;
  end

  def foo(s::string) =
    print ("foo: " + s);
  end

  def bar(s::int) -> int =
    s + 10;
  end
end
